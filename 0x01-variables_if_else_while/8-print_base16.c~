#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <stdio.h>

/**
*main- prints alphabets in lowercase
*Return: 0
*/
int main(void)
{
int i;
char j;
for (i = 1; i < 10; i++)
{
putchar(i + '0');
}
for (j = 'a'; j < 'g'; j++);
{
putchar(j);
}
putchar('\n');
return (0);
}
