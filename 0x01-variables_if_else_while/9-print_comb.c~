#include <stdlib.h>
#include <string.h>
#include <time.h>
#include <stdio.h>

/**
*main- prints out hex numbers
*Return: 0
*/
int main(void)
{
int i;

for (i = 0; i < 10; i++)
{
putchar(i + '0');
putchar(' ');
putchar(',');
}
putchar('\n');
return (0);
}
