#include "holberton.h"

/**
 * more_numbers - prints 1 to 14 10 times
 * Return: void
 */
void more_numbers(void)
{
int x;
int i;
for ( = 0; i < 10; x++)
{
for (i = 0; i <= 14; i++)
{
  printf("i at this poitn i %d", i);
  if (i < 9)
    {
      putchar(i + '0');
    } 
  else if (i >= 10)
    {
      int t = i % 10;
     _putchar(1 + '0');
     _putchar(t + '0');
    }
}
putchar('\n');
}
return;
}
