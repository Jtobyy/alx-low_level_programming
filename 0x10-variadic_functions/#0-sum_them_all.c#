#include "variadic_functions.h"

int sum_them_all(const unsigned int n, ...)
{
  va_list ap;
  int i;
  int sum;
  va_start(ap, n);z
  for (i = 0; i < n; i++)
    sum += va_arg(ap, n);
if (n == 0)
return (0);

  
}
