#include "lists.h"

/**
 *find_listint_loop - Returns the address of the
 *node where the loop starts, or NULL if there is no loop
 *@head: head of list
 *Return: address of node wher loop starts
 */
listint_t *find_listint_loop(listint_t *head __attribute__((unused)))
{
return (NULL);
}
